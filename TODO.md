# TODO

## UI: Reduce excessive black (approved direction)
- [ ] Replace hardcoded dark backgrounds (`#121212`, `rgba(0,0,0,...)`, `rgba(5,5,5,...)`, etc.) with token-driven `glass-*` / `bg-card` / `border-border` styles.
- [ ] For top shell components: update `src/components/Sidebar.jsx`, `src/components/Navbar.jsx`, `src/components/BottomNav.jsx`, `src/components/player/MiniPlayer.jsx`, and `src/pages/Welcome.jsx` to reduce “too much black”.
- [ ] Ensure contrast remains readable for text.
- [ ] Re-run `npm run build`.

